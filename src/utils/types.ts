export type Product = {
    id: number,
    title: string,
    description: string,
    category: string,
    images: string[],
}

export type PopularPage = {
    id: number;
    title: string;
    url: string;
};